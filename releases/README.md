# Release artifacts

This directory retains packaged application artifacts and earlier bundles for provenance. The canonical development source is `apps/live-commerce`.

Rebuild the current wheel from the project root with:

```bash
make wheel
```

Create complete project archives with:

```bash
make package
```
